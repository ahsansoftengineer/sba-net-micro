{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to APIGateway",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:pickProcess}",
      "symbolSearchPath": "${workspaceFolder}/SBA.APIGateway/bin/Debug/net8.0"
    },
    {
      "name": "Attach to Auth",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:pickProcess}",
      "symbolSearchPath": "${workspaceFolder}/SBA.Auth/bin/Debug/net8.0"
    },
    {
      "name": "Attach to Hierarchy",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:pickProcess}",
      "symbolSearchPath": "${workspaceFolder}/SBA.Hierarchy/bin/Debug/net8.0"
    }
  ],
  "compounds": [
    {
      "name": "Attach to All Microservices",
      "configurations": [
        "Attach to APIGateway",
        "Attach to Auth",
        "Attach to Hierarchy"
      ]
    }
  ]
}
